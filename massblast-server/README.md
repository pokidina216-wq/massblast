# MassBlast

## Файлы
- `server.js` — сервер (Node.js)
- `package.json` — зависимости
- `public/index.html` — сайт

## Переменные окружения (Railway)
- `ADMIN_PASSWORD` — пароль для админ-панели (по умолчанию: admin1234)
- `PORT` — порт (Railway ставит сам)
